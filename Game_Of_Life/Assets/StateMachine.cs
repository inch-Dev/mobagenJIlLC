using UnityEngine;

public class StateMachine : MonoBehaviour
{
    State m_currentState;
    public State GetCurrentState(){ return m_currentState; }
    bool UpdateStates(Agent agent)
    {

        
        return false;
    }
}
